[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2"]

[project]
name = "gfelib"
version = "0.0.1"
description = "A collection of reusable components for gdsfactory"
requires-python = ">=3.11"
dependencies = [
  "gdsfactory",
  "numpy"
]
